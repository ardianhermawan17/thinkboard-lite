// The barrel is the contract (I8). The painter is deliberately NOT exported: imperative Konva stays inside
// the leaf (I17/I18).
export { Marquee } from "./marquee"
export type { MarqueeProps, MarqueeStroke, MarqueeStyle, MarqueeTool } from "./types"
