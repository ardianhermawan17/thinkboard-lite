{
  "task_id": "030-task-db-housekeeping",
  "phase": "result",
  "closed_at": "2026-09-26T08:30:00Z",
  "final_status": "done",
  "goals_summary": {
    "total": 4,
    "done": 4,
    "blocked": 0,
    "detail": "g1 the anon EXECUTE revoke migration pass (proven live); g2 the co-member profiles policy pass (proven live); g3 the suite checks pass (19/19) and are first-class assertions; g4 the docs marked done pass. Recomputed from task.json goals[] and validate.json goal_checks — all four 'pending' -> 'done', none blocked."
  },
  "artifacts": [
    "database-thinkboard-lite/supabase/migrations/0007_revoke_anon_execute.sql",
    "database-thinkboard-lite/supabase/migrations/0008_team_profile_names.sql",
    "database-thinkboard-lite/supabase/tests/rls.sql — tb_test.anon() + the g17/g18 checks + the anon grants",
    "database-thinkboard-lite/supabase/tests/rls.test.mjs — g17/g18 asserted individually",
    "database-thinkboard-lite/supabase/config.toml — ports 563xx",
    "README.md §6, database-thinkboard-lite/README.md, 06-whole-apps-task.md (PHASE G · 030), todo-task-030-db-housekeeping.md"
  ],
  "verify": "LIVE and green: `npm test` (RLS) 19/19 including g17/g18; `npm run test:seed` 6/6. The frontend gate is unchanged: verify:arch 18 pass/0 fail, lint 0 errors, typecheck clean, vitest 360 passed/10 skipped. The Lite stack was moved 553xx -> 563xx to escape Windows' reserved 55262-55361.",
  "decisions_recap": [
    "EXECUTE is revoked from PUBLIC and anon and granted to authenticated, with matching default privileges — proven live.",
    "The profiles policy grants co-member reads only, via a shared-team join — proven live.",
    "The Lite stack's ports moved to 563xx because Windows reserves the documented 553xx range on this machine.",
    "The tb_test harness grants anon USAGE/EXECUTE so the g17 checks can run as anon."
  ],
  "follow_up_tasks": [
    "If the pilot machine does not reserve 553xx, the documented ports can be restored (config.toml + READMEs + two comments).",
    "The live frontend suites (sync-engine.live, workspace-remote) can now run against the 563xx stack with NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:56321."
  ]
}
