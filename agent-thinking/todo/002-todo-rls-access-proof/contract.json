{
  "id": "002-todo-rls-access-proof",
  "name": "RLS access proof",
  "human_prompt": "continue with proper ordering. Yes add g14-g16. You should check \"database-thinkboard-lite\" is it any file/folder that i forgot to add? I devide the backend and database. Because later i think it will be needed for Golang backend. (database, backend). no i already start desktop. change to branch \"claude/task-002\"",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 002 per todo-task-002-rls-access-proof.md, on branch claude/task-002 (cut from origin/master).\n1. Goals g1-g16; g14-g16 were added at analyze by the owner and are in the plan doc, 02 §9 and the JSON skeletons.\n2. Tests live in database-thinkboard-lite/supabase/tests/: sql/ (plain SQL through docker exec psql, each in a rolled-back transaction under `set local role authenticated` with request.jwt.claims set) and realtime/ (node:test, two signed-in supabase-js clients). Seeding runs as postgres (clients cannot add team members).\n3. The Lite stack (supabase start with the same -x exclusions as task 001, ports 553xx) must be up; Docker Desktop is started by the owner.\n4. The realtime half needs @supabase/supabase-js as a devDependency in database-thinkboard-lite/ — ask the owner before npm install.\n5. A red test is a finding for 001, not a reason to weaken a policy: stop and show the owner before editing 0004_lite.sql / 02 §8.1 (they stay byte-identical).\n6. Audit of database-thinkboard-lite/ for missing files: seed.sql (003), tests/ (002), README.md + CLAUDE.md, package.json scripts. The owner splits database from backend for a future Go backend; the Lite `backend` architecture stays frontend-thinkboard-lite/src/server/**.\n7. test.json.summary.total >= 16. No git write without an explicit yes (09-agent-limitation.md); root repo only."
}
