{
  "id": "005-todo-swagger-backend-api",
  "name": "Add Swagger to backend and implement api.json",
  "human_prompt": "You must read README.md first for contextual.\n\n1. Your task is adding \"Swagger\" to the backend\n2. Implement the API in \"thinkboard-backend/api/api.json\"\n3. Make sure to add test validation with : email: admin@gmail.com, password: password",
  "translated_prompt_agent": "Read README.md first for repo context, then follow 02-working.md's analyze -> code -> test -> validate -> result cycle under agent-history/ (next task id continues the monotonic sequence after 004-task-postman-api-collection).\n\n1. Add Swagger/OpenAPI documentation to thinkboard-backend. thinkboard-backend/api/openapi.yaml already exists as a stub (only /healthz) - extend it to cover the actual current route surface (see internal/auth, handler/me.go, router.go), and wire up a swagger UI/doc-serving route in the Go gateway if the plan/backend-folder-architecture doc supports it.\n2. thinkboard-backend/api/api.json is the existing Postman collection (not OpenAPI) - reconcile with the task instruction: either treat 'Implement the API in api.json' as 'keep the Postman collection in sync with the routes documented in openapi.yaml', or confirm with the user if a different api.json format was intended. Do not silently redefine api.json's existing format.\n3. Add a test using email admin@gmail.com / password password to validate the auth flow (Supabase sign-in) end-to-end against the documented/implemented API surface.\n\nFollow CLAUDE.md and 02-working.md task tracking contract throughout; do not skip the analyze phase before writing code."
}
