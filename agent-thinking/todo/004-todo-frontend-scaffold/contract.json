{
  "id": "004-todo-frontend-scaffold",
  "name": "Conform the frontend scaffold",
  "human_prompt": "continue task 004",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 004 per todo-task-004-frontend-scaffold.md, on branch claude/task-004 (cut from origin/master, after PRs #4 and #5 merged).\n1. Goals g1-g6: conform the existing create-next-app scaffold (C9, never regenerate): move app/, components/, lib/, hooks/ under src/; four aliases in tsconfig.json AND vitest.config.mts; spread eslint.architecture.mjs into eslint.config.mjs; src/{app,features,shared} skeleton and components.json rebound; Vitest (jsdom + fake-indexeddb), Storybook + Playwright; scripts verify:arch/lint/typecheck/test/verify; Serwist; verify:arch green on the real tree and failing without src/.\n2. Owner answers 2026-09-19 (plan approved with the recommended options): ESLint is downgraded to 9 so eslint-config-next works (task doc §1, raised, not silently picked); npm install is allowed for the tooling dependencies; work on a new branch claude/task-004 from origin/master.\n3. Read node_modules/next/dist/docs/ before touching Next config (AGENTS.md). No product code. Redux Toolkit is the only store library (C8), and it is not added here.\n4. Gate: npm run verify green on the real tree. test.json.summary.total > 0. No git write without an explicit yes (09-agent-limitation.md); root repo only, never master."
}
