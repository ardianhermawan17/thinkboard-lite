{
  "id": "008-todo-auth-and-workspace-shell",
  "name": "Auth and workspace shell",
  "human_prompt": "okay continue go with this order",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 008 per todo-task-008-auth-and-workspace-shell.md, on branch claude/task-008 (cut from claude/task-005 because PR for 005 is not merged yet).\n1. Goals g1-g6: Supabase auth (publishable key) with a locally cached session; workspace create via the create_workspace RPC; members list and leadership transfer via transfer_leadership; workspace and personal persona editors; WorkspaceShell with app-header and auth-guard in features/workspace; the workspace route as a client component loaded ssr:false.\n2. Order agreed 2026-09-20 (plan approved): gates 1-3 slim, analyze, session slice via the 4-point store edit, auth, workspace create, members and transfer, personas and shell, tests (create atomicity, offline reopen, one leader after transfer, gate test against seeded users under real RLS), verify + webpack build + Chrome boot, slim result and one Notion entry.\n3. Defaults taken because the human said 'go with this order': create_workspace is called directly (online) now, 017's endpoint will wrap the same RPC later; the anon EXECUTE revoke on the definer RPCs is a separate follow-up task; adding members to a team is a follow-up.\n4. No git write without an explicit yes; never master."
}
