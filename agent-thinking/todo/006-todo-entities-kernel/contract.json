{
  "id": "006-todo-entities-kernel",
  "name": "Entities kernel",
  "human_prompt": "review task-003, task-002, task-001. If it valid plan next task | yes, go with your recomendation.",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 006 per todo-task-006-entities-kernel.md, on branch claude/task-006 (cut from origin/master after PR #6 merged 004 and the 003 follow-up).\n1. Goals g1-g6: the Dexie kernel per 02 section 6.1 (seven tables, three-state _sync, per-profile name thinkboard:{profileId}), repository/ writes (row + outbox in one transaction), queries/ live-query hooks (one per table, useLiveQuery only there), utils/mappers.ts (toWire/toRow, notes <-> highlight_notes, local-only fields never cross), apply-remote.ts (no outbox echo, skips non-clean rows, delete keyed on record ?? old_record, RETRACT), tests under fake-indexeddb.\n2. Owner answers 2026-09-19 (recommendations accepted): install dexie and dexie-react-hooks; hand-write the uuidv7 generator (no library); commit and merge 003 + 004 first (done: PR #6); SLIM recording for this task: analyze.json and one short result.json plus task.json and running-process; code/test/validate collapse into one `work` phase and git plus the tests are the evidence.\n3. Reuse the generated domain types (src/shared/types/domain/*) for the row shapes; no product code beyond the kernel; entities imports nothing from other features (I11, I12, I23, I29).\n4. Gate: npm run verify green (I13 active once db/thinkboard-db.ts exists; I29 dexie only in db/, repository/, queries/). No git write without an explicit yes; never master."
}
