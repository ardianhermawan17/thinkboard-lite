{
  "id": "009-todo-start-run-analytic-stub",
  "name": "StartRun service with stub analytic stage",
  "human_prompt": "010 — StartRun service with stub analytic stage\n\n010-task-start-run-analytic-stub\n\nFocus:\n\n- pipeline.Service.StartRun\n- one stubbed analytic stage\n- no real LLM provider\n- fake dependencies\n- maybe store interface, but minimal DB implementation only if needed\n\nThis maps to backend architecture build order step 4.\n\nInstructions:\n1. Read README.md\n2. Focus on making task above\n3. Group task of \"task-pipeline-domain-core\"\n4. Ask first for validation, and then use /ponytail\n5. Make sure to documented in \"TODO\" and \"HISTORY\".\n6. Ask first when want to push",
  "translated_prompt_agent": "Read README.md, then 03-backend-folder-architecture.md §5.1/§9 build order step 4, for orchestration-shape context before touching code.\n\n1. Task numbering: the goal text says '010' but agent-history's monotonic counter is at 008 (next free: 009) with no external branch claiming 009 — use 009-task-start-run-analytic-stub, same precedent as task 006's renumbering. Confirm this reasoning is still acceptable rather than silently deviating from 02-working.md §3's monotonic rule.\n2. Add internal/pipeline/deps.go with a minimal LLM interface (Complete(ctx, prompt string) (string, error)) — no Retriever/Memory/Personas yet, nothing calls them.\n3. Add a stubbed analytic stage function that calls LLM.Complete once and wraps the result as a single root Point (reuse task 008's Point/NewPoint).\n4. Add pipeline.Service with NewService(llm, clock) and StartRun(ctx, StartRunCmd) (kernel.RunID, error): validate Content is non-empty, create+Start a Run (task 007's Run), Advance past scope_anchor (no handler — context-warning gate is a later task), run the analytic stub, Advance past analytic on success (or Run.Fail on LLM error), keep state in-memory (ask the user first: skip a Store interface entirely, per ponytail's no-single-implementation-interface rule, unless they want one built ahead of a real consumer).\n5. Do not add orchestrator.go or the stages/ subpackage yet — one stage handler doesn't justify the generic Stage->Handler registry, and it would reintroduce the import-cycle problem already flagged and resolved in task 006's analyze.json.\n6. Write pipeline/service_test.go using a fake LLM: success path, LLM-failure path (run recorded as failed), empty-content rejection.\n7. Track via 02-working.md's analyze -> code -> test -> validate -> result cycle under agent-history/, AND register this contract here in agent-thinking/todo/ + tracking-todo.json (the user explicitly asked for both TODO and HISTORY documentation this time, not just agent-history).\n8. Do not push to master without asking first and getting an explicit yes, per 05-agent-limitation.md §1 (confirmation is required every time, not just once per session)."
}
