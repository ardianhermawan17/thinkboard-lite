{
  "id": "005-todo-design-system",
  "name": "Design system",
  "human_prompt": "continue for right ordering. 005",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 005 per todo-task-005-design-system.md, on branch claude/task-005 (cut from origin/master after PRs #6 and #7 merged 004, 003 and 006).\n1. Goals g1-g5: the shadcn atom set (button, input, textarea, sheet, dialog, tabs, card, skeleton, sonner, tooltip, switch); highlight colour tokens for light AND dark in globals.css; LibraryProvider in the fixed order Redux -> PersistGate -> Shadcn -> Theme -> Tooltip with the Toaster mounted once; useBreakpoint() in shared/hooks as the only device branch; the store seam (stripUi transform, persistConfig, listenerMiddleware registered: the 4-point store edit).\n2. Owner answers 2026-09-19 (plan approved): empty store seam, no slices in this task (session, workspace, viewport, sync arrive with 008, 009, 007 via the 4-point edit); highlight tokens are yellow, green, blue, rose (one hue each, same hue in both themes, lower alpha in dark); install @reduxjs/toolkit, react-redux, redux-persist and sonner and let shadcn add generate the atoms; slim recording (analyze.json + result.json); PR #7 merged first.\n3. First technical check: components.json aliases were rebound to filesystem-style paths; verify how the shadcn CLI resolves them (dry run) before adding anything, and change components.json only if it resolves wrongly.\n4. Gate: npm run verify green (I5/I11/I12/I16 become active with store.ts) and the app boots light and dark; no hex colour under shared/components; Redux Toolkit only, no Zustand or TanStack Query (C8). No git write without an explicit yes; never master."
}
