{
  "id": "007-todo-sync-kernel",
  "name": "Sync kernel",
  "human_prompt": "[007 sync kernel] I already merged task 5 and 8 in master, continue to 007 task. Then: Use your default,",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 007 per todo-task-007-sync-kernel.md, on branch claude/task-007, stacked on claude/task-008 (origin/master has task 005 only; task 008 is not merged yet, checked 2026-09-20).\n1. Goals g1-g7: outbox push (strict seq order, failed head skipped, parked parent parks its children), coalesce + backoff, realtime channel (setAuth before subscribe and on refresh, both topics, RETRACT, bursts in one transaction), manifest diff and paginated bootstrap (push -> pull -> resubscribe), sync slice + selectors + listener middleware (4-point store edit) + sync-status-pill, tests, and pulling the non-mirrored tables into Dexie meta.\n2. Defaults taken because the human said 'use your default': transfer_leadership and the persona saves become outbox ops (DB-Q12 default); create_workspace stays online-only (it needs the new session id to navigate; offline create is undefined); 008's one-shot pull and its own sync pill are replaced by the sync engine and sync-status-pill; sign-out refuses while ops are queued; g1-g4 are timeboxed and the PowerSync escape hatch (Q10) is raised to the human before adding anything if the reconnect-order tests will not go green.\n3. Gate: two browsers on the seed workspace see each other's rows; three offline writes land once after reconnect; a 403'd op parks without stalling the rest. No git write without an explicit yes; never master."
}
