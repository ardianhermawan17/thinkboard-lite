{
  "id": "010-todo-text-highlight",
  "name": "Text highlight",
  "human_prompt": "Continue the task, make sure to track the progress trough \"obsidian\" (for now), use all obsidian feature such as \"kanban\"",
  "translated_prompt_agent": "Follow the main gate in README.md. Task 010 per todo-task-010-text-highlight.md.\n1. Goals g1-g5: Selection/Range capture over the PDF.js text layer -> client rects -> normalized rects; deterministic slug h-pNN-NN-xxxxxx; repository write (insertHighlight, extraction='text_layer', confidence=1.0, layer='individual'); a highlight-layer canvas leaf with a painter that draws stored highlights from the live query; a zoom-then-reload test.\n2. Gate 2 passed: 009 is done. Section 3 has no open decision row (RULE-09/16/17 govern it).\n3. Plan approved 2026-09-22: reuse the existing insertHighlight() repository function and useHighlightsForPage() query (both already exist from tasks 006/007) rather than adding new ones; reuse shared/utils/geometry.ts from task 009 for all coordinate math; the slug's `order` component needs a determinism decision (reading-order rank vs insertion order), to be resolved in analyze and recorded, not silently guessed.\n4. Progress is now also tracked in the Obsidian vault (wsl:~/thinkboard-lite-vault, materialized to obsidian-out/): each task page's task_status property, the native Bases 'Task Board', and the Kanban-plugin-format 'Kanban Board.md' should move together with the repo's own agent-history state, not just at close.\n5. No git write without an explicit yes; never master."
}
